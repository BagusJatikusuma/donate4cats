CREATE TABLE IF NOT EXISTS creators(id VARCHAR(50) PRIMARY KEY, member_id VARCHAR(50) NOT NULL, username VARCHAR(100) NOT NULL, display_name VARCHAR(500) NOT NULL, bio LONGTEXT, created_at DATETIME NOT NULL, FOREIGN KEY (member_id) REFERENCES members(id));