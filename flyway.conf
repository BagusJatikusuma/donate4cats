# --- Database connection ---
flyway.url=jdbc:mysql://localhost:3306/donate4cats
flyway.user=root
flyway.password=root

# --- Migration files location ---
# By default Flyway looks in sql/ under the installation dir.
# You can change it to match your project structure:
flyway.locations=filesystem:sql

# --- Optional settings ---
# Schema to manage (defaults to the user’s default schema)
flyway.schemas=donate4cats

# Baseline if you already have an existing DB
flyway.baselineOnMigrate=true

# Clean disabled in production for safety
flyway.cleanDisabled=true